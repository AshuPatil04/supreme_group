/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'supreme-group.vercel.app'],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;